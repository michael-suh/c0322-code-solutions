/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}
// enter string value in the parameter
// string value at index string.length - 1 (last character of string) is being returned from the function
