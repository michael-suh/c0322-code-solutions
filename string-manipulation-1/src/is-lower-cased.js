/* exported isLowerCased */
function isLowerCased(word) {
  if (word === word.toLowerCase()) {
    return true;
  } else {
    return false;
  }
}

// use conditionals to check if the whole word is lowercased.
// return true if it is, and return false if it is not.
