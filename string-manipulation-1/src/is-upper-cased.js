/* exported isUpperCased */
function isUpperCased(word) {
  if (word === word.toUpperCase()) {
    return true;
  } else {
    return false;
  }
}

// use conditionals to check if the whole word is uppercased.
// return true if it is, but if not, return false
